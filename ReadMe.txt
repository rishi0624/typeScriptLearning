1. What Is Type Script
ans : TypeScript is a superset of javascript which is maintain by Microsost Company. Every Valid javascript code is valid in type script and it viceVersa. When typescript compile the code it convert the code in to plain javascript so browser can understand it .

2. What is type script features. 

    1. Strong Typing: Suppose if i write a variable in type script , then we can set its type.

    ex: var message:string ="hello World"
        console.log("message")

    2. Object Oriented Classes: 
    3. Complie time Error:
    4. Angular 2 and above framework rewritten in full type script.

3. How to install TypeScript ?
ans: 
    Step 1. First install node.js on your system.
    step 2. npm install -g typescript 

    Note: tsc stand for  type script compiler

4. How to check node JS version ?
ans: node --version

5. How to check typescript Version ?
ans: tsc --version

6. How to compile ts file .
ans: tsc filename
    ex: tsc type.ts   // and this command compiler type.ts file in type.js file.
    NOTE: We include only type.js file in index file not type.ts file.
    